RewriteEngine on
RewriteCond %{HTTP_HOST} ^(www.)?praivainc.com$
# Cambiar 'subdirectory' por el nombre del subdirectorio que quiere usar
RewriteCond %{REQUEST_URI} !^/praivainc.com/public/
# No cambiar estas lineas.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
# Cambiar 'subdirectory' por el nombre del subdirectorio que quiere usar
RewriteRule ^(.*)$ /praivainc.com/public/$1
# Cambiar sudominio.com por su nombre de dominio
# Cambiar 'subdirectory' por el nombre del subdirectorio que quiere usar
# followed by / then the main file for your site, index.php, index.html, etc.
RewriteCond %{HTTP_HOST} ^(www.)?praivainc.com$
RewriteRule ^(/)?$ praivainc.com/public/index.php [L]

RewriteEngine on
RewriteCond %{HTTP_HOST} ^(www.)?youparty.com.ve$
# Cambiar 'subdirectory' por el nombre del subdirectorio que quiere usar
RewriteCond %{REQUEST_URI} !^/praivainc.com/public/
# No cambiar estas lineas.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
# Cambiar 'subdirectory' por el nombre del subdirectorio que quiere usar
RewriteRule ^(.*)$ /praivainc.com/public/$1
# Cambiar sudominio.com por su nombre de dominio
# Cambiar 'subdirectory' por el nombre del subdirectorio que quiere usar
# followed by / then the main file for your site, index.php, index.html, etc.
RewriteCond %{HTTP_HOST} ^(www.)?praivainc.com$
RewriteRule ^(/)?$ praivainc.com/public/index.php [L]